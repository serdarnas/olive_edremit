# Kopyala: cp .env.example .env — sonra değerleri doldur. `.env` git'e GİRMEZ.

# Telegram botu: @BotFather ile bot aç, token'ı buraya yapıştır.
TELEGRAM_BOT_TOKEN=
# Bota bir mesaj at, sonra `python3 bin/telegram_oku.py --chat-id-bul` çalıştır, çıkan sayıyı yaz.
TELEGRAM_CHAT_ID=

# Apify (YouTube verisi): apify.com → Settings → API tokens
APIFY_TOKEN=

# fal (kapak görseli): fal.ai → Keys. Boş bırakılabilir — kapağı sen eklersin.
FAL_KEY=

# Bekçi için ayrı aile (ANAYASA 3). Boş bırakılırsa yedek yol Haiku'dur ve karar
# "bekçi aynı aileden — uyarı" notuyla kaydedilir.
OPENAI_API_KEY=

# İzlenecek YouTube kanalı (youtube-analiz takımı bunu okur)
KANAL=@ornek-kanal

# Ürün XML beslemesi (urun-pazarlama takımı bunu okur). Gizli değil — bkz.
# sirket/KURUMSAL-BILGILER.md "Ürün veri kaynağı"; yine de tek yer .env olsun diye buradan okunur.
NIDA_XML_URL=

# Facebook/Instagram Graph API (marka-izleme okur — SALT OKUMA; yayın/reklam ucu hiç çağrılmaz).
# Meta for Developers → Graph API Explorer'dan token al. Boş bırakılırsa marka-izleme yalnız
# WebSearch ile devam eder, koşu bloklanmaz.
FACEBOOK_ACCESS_TOKEN=
# Boş bırakılırsa bin/facebook_veri_cek.py token ile /me/accounts çağrısı yapıp keşfetmeyi dener.
FACEBOOK_PAGE_ID=
INSTAGRAM_BUSINESS_ID=

# TikTok — henüz KULLANILMIYOR (geliştirici uygulaması onay bekliyor, access token yok).
# Uygulama onaylanıp OAuth token alınınca gerçek entegrasyon ayrı bir iş olarak eklenir.
TIKTOK_CLIENT_KEY=
TIKTOK_CLIENT_SECRET=

# --- farklı model (opsiyonel, bkz. docs/07-farkli-model.md) ---
# NVIDIA'nın ücretsiz model kataloğu: build.nvidia.com → hesap aç → bir model kartında
# "Get API Key" → çıkan `nvapi-…` anahtarını buraya yapıştır. Boşsa hiçbir şey değişmez:
# bütün takımlar Anthropic'te koşmaya devam eder.
NVIDIA_API_KEY=
# Koşacak NIM modeli, ör. meta/llama-3.3-70b-instruct
# TOOL-USE (fonksiyon çağrısı) desteklemeyen model ajanı çalıştıramaz — model kartına bak.
NIM_MODEL=
